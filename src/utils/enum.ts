export enum Level {
  Easy = 30,
  Medium = 20,
  Hard = 10,
}

export enum Language {
  Turkish = "tr-TR",
  English = "en-US",
}

export enum PlayerType {
  Computer = "Computer",
  User = "User",
}
