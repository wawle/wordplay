export enum Level {
  easy = 30,
  normal = 20,
  veteran = 10,
}

export enum Language {
  Turkish = "tr-TR",
  English = "en-US",
}

export enum PlayerType {
  Computer = "Computer",
  User = "User",
}
